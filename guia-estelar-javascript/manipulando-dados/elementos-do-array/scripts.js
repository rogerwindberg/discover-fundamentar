// Manipulando Arrays

// Contar elementos de um array
console.log(["a", "b", "c"].length)

// array também funciona com qualquer tipo de objeto
console.log([
    "a",
    25,
    {type: "array"},
    function() { return "alo"},
].length)